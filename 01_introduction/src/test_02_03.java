import java.util.Scanner;

public class test_02_03 {
  public static void main(String[] args) {
	 // 2-4. 문자열을 입력받아 앞에서 세글자까지 출력하시오
/*
	  Scanner sc = new Scanner(System.in);
	  
	  String str1 = sc.nextLine();
	  char p1 = str1.substring(0,1);      // = str1.charAt(0);
	  char p2 = str1.substring(1,2);		// = str1.charAt(1);
	  char p3 = str1.substring(2,3);		// = str1.charAt(2);
	  
	  
	  System.out.println(p1);
	  System.out.println(p2);
	  System.out.println(p3);
*/
	  // 3-1. 입력 받은 하나의 정수가 양수이면 양수다 , 양수가 아니면 양수가 아니다 를 출력하세요
/*
	  Scanner sc = new Scanner(System.in);
	  int i = Integer.parseInt(sc.nextLine());
	  String str1 = i>=0 ? "양수다" : "양수가 아니다";
	  System.out.println(str1);
*/
	  //3-2. 키보드로입력 받은 하나의 정수가 양수이면 양수다 ,양수가 아닌 경우 중에서 0 이면 0 이다 , 0 이 아니면 음수다 를 출력하세요 (연산자만 사용할것)
/*
	  Scanner sc = new Scanner(System.in);
	  int i = Integer.parseInt(sc.nextLine());
	  String str1 = i>0 ? "양수다" : i == 0 ? "0이다" : "음수다";
	  System.out.println(str1);
*/
	  //3-3키보드로 입력 받은 하나의 정수가 짝수이면 짝수다 , 짝수가 아니면 홀수다 를 출력하세요
/*
	  Scanner sc = new Scanner(System.in);
	  int i = Integer.parseInt(sc.nextLine());
	  String str1 = i % 2 == 0 ? "짝수다":"홀수다";
	  System.out.println(str1);
*/
	  //3-4모든사람이 사탕을 골고루 나눠가지려고 한다 . 인원 수와 사탕 개수를 키보드로 입력 받고 1인당 동일하게 나눠가진 사탕 개수와 나눠주고 남은 사탕의 개수를 출력하세요
/*	  
	  Scanner sc = new Scanner(System.in);
	  
	  System.out.println("사탕수를 입력하시오");
	  int i1 = Integer.parseInt(sc.nextLine());
	  System.out.println("사람수를 입력하시오");
	  int i2 = Integer.parseInt(sc.nextLine());
	  int i3 = i1/i2;
	  int i4 = i1-(i3*i2);
	  
	  System.out.println(i3 + "씩 나눠갖고 " + i4 + "개 남았습니다.");
*/
	  //3-5
	  
	  
	  
	  
	  
	  
		  
	  
	  
  }
}
